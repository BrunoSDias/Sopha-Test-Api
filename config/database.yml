default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: johnversion
  password: SENHA
  host: <%= ENV['DATABASE_HOST'] || 'localhost' %>
  port: <%= ENV['DATABASE_PORT'] || '5432' %>

development:
  <<: *default
  database: sophatestapi_development

test:
  <<: *default
  database: sophatestapi_test

production:
  <<: *default
  database: sophatestapi_production
